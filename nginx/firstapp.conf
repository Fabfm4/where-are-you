upstream flask {
  ip_hash;
  server flask:80;
}

server {

    location /static/ {
        autoindex on;
        alias /media/assets/;
    }

    location / {
        proxy_pass http://flask/;
    }
    listen 80;
    server_name localhost;
}
